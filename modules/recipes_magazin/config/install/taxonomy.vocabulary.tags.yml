langcode: en
status: true
dependencies:
  enforced:
    module:
      - recipes_magazin
name: Tags
vid: tags
description: 'Use tags to group articles on similar topics into categories.'
hierarchy: 0
weight: 0
